{
  "name": "back-end",
  "version": "1.0.0",
  "description": "backend for the basic forum site",
  "main": "build/index.js",
  "scripts": {
    "start": "npm run build && node ./build/index.js",
    "build": "rimraf ./build && npx tsc",
    "dev": "nodemon --exec ts-node ./src/index.ts"
  },
  "keywords": ["graphql", "typescript", "express"],
  "author": "Daniel",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "apollo-server-express": "^3.12.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "graphql": "^16.8.1"
  }
}
